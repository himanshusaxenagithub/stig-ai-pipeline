# Assess fixtures

`scan_mixed.json` is a compact stig-scan report used by the module 3
tests. It is not a live host capture. Passing, failing, error, manual,
skipped and untrusted rows are all present so the drafter's refusals
can be asserted.

`checklist.json` supplies filed explanations and fix text for two of
the failing rules.
